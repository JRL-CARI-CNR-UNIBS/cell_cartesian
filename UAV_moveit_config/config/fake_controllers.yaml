controller_list:
  - name: fake_UAV_arm_controller
    type: $(arg fake_execution_type)
    joints:
      - joint_X
      - joint_Y
      - joint_Z
initial:  # Define initial robot poses per group
  - group: UAV_arm
    pose: home